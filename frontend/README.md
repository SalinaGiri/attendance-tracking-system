# Team31 Front-end

## Prerequisites

Install all required node dependencies using npm:

```console
> npm install
```

Create a .env file with the following content:

```
NEXT_PUBLIC_API_URL="http://localhost:3000"
```

## Starting the application

To start the front end execute:

```console
> npm run dev
```
